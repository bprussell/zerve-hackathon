# Large data files — keep raw CSVs in git (small), ignore bulky downloads
data/raw/*.xlsx
data/raw/*.zip
# data/processed/ — tracked (small derived CSVs)

# Claude Code local settings
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/
